[build-system]
requires = ["setuptools>=42", "wheel", "pybind11"]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
# Build cspice library first
before-build = "python {project}/get_spice.py"